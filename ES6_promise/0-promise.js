export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // Hazırda bizdən sadəcə Promise qaytarmaq tələb olunur, 
    // ona görə də içi boş ola bilər və ya birbaşa resolve() edə bilərik.
  });
}
